import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { TypePage } from './type';

@NgModule({
  declarations: [
    TypePage,
  ],
  imports: [
    IonicPageModule.forChild(TypePage),
  ],
})
export class TypePageModule {}
